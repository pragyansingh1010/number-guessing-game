# Game Rules

The game selects a hidden number, accepts guesses, and gives directional feedback until the player finds it or reaches the attempt limit.

A round should reset its state cleanly and keep the score independent from the previous round.