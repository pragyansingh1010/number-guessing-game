# Gameplay Validation

The target should be generated before the first guess and remain unchanged until the round ends. Inputs outside the configured range should be rejected, and each valid guess should update the attempt counter exactly once.